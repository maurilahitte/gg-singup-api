import { GetHealthStatus } from './health.types';

export const getHealthStatus = (): GetHealthStatus => {
    // TODO: Insert your health status logic in here
    return {
        status: 200,
    };
};
